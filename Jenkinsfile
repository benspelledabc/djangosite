pipeline {
    agent { dockerfile true }
    stages {
        stage('Test') {
            steps {
                echo 'hello world'
                echo 'playing with jenkins'
            }
        }
    }
}
